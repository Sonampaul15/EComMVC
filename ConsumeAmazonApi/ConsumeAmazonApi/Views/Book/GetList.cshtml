@model IEnumerable<ConsumeAmazonApi.DTO.BooksDto>
<div class=" container">
    <div class="row">
        <div class="col-md-10 offset-1">
            <table class="table table-bordered">
                <thead>
                    <tr class="text-center bg-secondary text-light">
                        <th>ID</th>
                        <th>BookName</th>
                        <th>Description</th>
                        <th>Price</th>
                        <th>Author</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr class="text-center">
                            <td>@item.Id</td>
                            <td>@item.BookName</td>
                            <td>@item.Description</td>
                            <td>@item.Price</td>
                            <td>@item.Author</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
